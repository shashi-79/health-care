{
  "name": "rural-health-copilot",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @rhc/web -- --hostname 0.0.0.0",
    "start": "npm run start --workspace @rhc/web -- --hostname 0.0.0.0",
    "build:packages": "npm run build --workspace @rhc/types && npm run build --workspace @rhc/safety && npm run build --workspace @rhc/triage && npm run build --workspace @rhc/policy && npm run build --workspace @rhc/tools && npm run build --workspace @rhc/ai && npm run build --workspace @rhc/medical && npm run build --workspace @rhc/db && npm run build --workspace @rhc/obs && npm run build --workspace @rhc/rag && npm run build --workspace @rhc/scheduler && npm run build --workspace @rhc/ingest && npm run build --workspace @rhc/agents && npm run build --workspace @rhc/worker",
    "build": "npm run build:packages && npm run build --workspace @rhc/web",
    "lint": "npm run lint --workspace @rhc/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "vitest": "4.1.4"
  },
  "overrides": {
    "eslint": "9.39.1"
  }
}
